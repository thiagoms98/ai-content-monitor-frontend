import { Check, Clock3 } from 'lucide-react';
import type { Post } from '@/lib/blog/moderation';
export function PostCard({ post }: { post: Post }) {
 return <article className="panel post-enter p-6 sm:p-8"><header className="mb-5 flex items-center gap-3"><div className="avatar">V</div><div><p className="font-semibold">Você <span className="ml-2 text-sm font-normal text-muted-foreground">@visitante</span></p><time className="text-sm text-muted-foreground" dateTime={post.createdAt}>{new Date(post.createdAt).toLocaleTimeString('pt-BR', {hour:'2-digit',minute:'2-digit'})} · nesta sessão</time></div><span className="ml-auto badge"><Check size={13}/> Aprovado</span></header>{post.text && <p className="whitespace-pre-wrap break-words leading-relaxed">{post.text}</p>}{post.image && <img className="mt-5 max-h-[520px] w-full rounded-xl object-contain bg-black/30" src={post.image} alt="Imagem do post aprovado"/>}<footer className="mt-6 flex items-center gap-2 border-t border-border pt-4 text-sm text-muted-foreground"><Clock3 size={14}/> Analisado em {(post.analysis.duration_ms / 1000).toFixed(2)} s <span className="ml-auto">Severidade {post.analysis.severity}</span></footer></article>;
}
